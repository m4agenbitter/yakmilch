apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/m4agenbitter/yakmilch:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/m4agenbitter/yakmilch:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: yakmilch
        chartPath: charts/yakmilch
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/m4agenbitter/yakmilch'
          image.tag: '{{.DIGEST_HEX}}'
